---
description: Test conventions for the ddlapi library.
globs: "test/**"
---

## Skill trigger: `ddlapi-testing`

When writing or changing tests under `test/**` for the ddlapi library —
`*.test.ts` files or the SQL fixtures under `test/resources/**` — apply the
`ddlapi-testing` skill.
